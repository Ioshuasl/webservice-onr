{
  "collectionUid": "COLE-AQUI-O-UID-DA-COLECAO-NO-POSTMAN",
  "collectionName": "cra webservice - n8n",
  "collectionFile": "postman/cra-webservice-n8n.postman_collection.json",
  "workspaceId": "",
  "comment": "Copie para postman/.postman-sync-cra.json (gitignored). UID em Share → Via API. API key: POSTMAN_API_KEY no .env ou vault env#Postman — API key (sync coleções). Push: npm run postman:sync:cra · create: npm run postman:sync:cra:create"
}
